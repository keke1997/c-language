#include <stdio.h>

int main()
{
    int a = 0;

    if (a > 10)
    {
        int b = 10;
    }
    else
    {
        int c = 10;
    }

    print("value of a, b and c: %d, %d, %d", a, b, c);

    return 0;
}